'use strict';

const materialUiComponents = require('..');
const assert = require('assert').strict;

assert.strictEqual(materialUiComponents(), 'Hello from materialUiComponents');
console.info('materialUiComponents tests passed');
