'use strict';

const antdComponents = require('..');
const assert = require('assert').strict;

assert.strictEqual(antdComponents(), 'Hello from antdComponents');
console.info('antdComponents tests passed');
